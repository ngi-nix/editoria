// const db = require('@pubsweet/db-manager/src/db')

const dbCleaner = async () => {
  // const tableNames = await db.raw(
  //   `SELECT tablename FROM pg_tables WHERE schemaname='public'`,
  // )
  // console.log('wha', tableNames)
}

module.exports = dbCleaner
