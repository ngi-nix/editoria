type ExportScript {
  label: String!
  value: String!
  scope: String!
}

input ExportScriptInput {
  label: String!
  value: String!
}

extend type Query {
  getExportScripts(scope: String): [ExportScript!]!
}
