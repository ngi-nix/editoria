type Division {
  id: ID!
  label: String
  bookComponents: [BookComponent]!
}

type Mutation {
  updateBookComponentOrder(
    targetDivisionId: ID!
    bookComponentId: ID!
    index: Int!
  ): [Division]!
}
